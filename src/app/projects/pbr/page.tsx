import PBRSection from "../../../components/projectsections/PBR"
export const metadata = {
  title: "PBR | Claire Lu"
}

export default function PBRPage() {
    return (
        <main>
        <PBRSection/>
        </main>
      );
}