import RobotSection from "../../../components/projectsections/Robot"
export const metadata = {
  title: "Stylized Robot Scene | Claire Lu"
}

export default function RobotPage() {
    return (
        <main>
        <RobotSection/>
        </main>
      );
}