import LegoSection from "../../../components/projectsections/Lego"
export const metadata = {
  title: "Lego-ifier | Claire Lu"
}

export default function LegoPage() {
    return (
        <main>
        <LegoSection/>
        </main>
      );
}