import FlickerSection from "../../../components/projectsections/Flicker"
export const metadata = {
  title: "Flicker | Claire Lu"
}

export default function FlickerPage() {
    return (
        <main>
        <FlickerSection/>
        </main>
      );
}