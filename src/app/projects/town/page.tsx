import TownSection from "../../../components/projectsections/Town"
export const metadata = {
  title: "Stylized Town Scene | Claire Lu"
}

export default function TownPage() {
    return (
        <main>
        <TownSection/>
        </main>
      );
}