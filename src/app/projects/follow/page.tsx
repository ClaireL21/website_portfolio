import FollowSection from "../../../components/projectsections/Follow"
export const metadata = {
  title: "Follow | Claire Lu"
}

export default function FollowPage() {
    return (
        <main>
        <FollowSection/>
        </main>
      );
}